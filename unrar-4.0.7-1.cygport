DESCRIPTION="RAR archive decompressor"
HOMEPAGE="http://www.rarlab.com/"
SRC_URI="http://www.rarlab.com/rar/unrarsrc-${PV}.tar.gz"
SRC_DIR=${PN}

src_compile() {
	lndirs
	cd ${B}
	cygmake -f makefile.unix STRIP=:
}

src_install() {
	cd ${B}
	dobin unrar.exe
	dodoc license.txt readme.txt
}
